#############################################
# xunji-frontend (前端) 环境变量示例
#############################################

# [必须/可选] 必须
# [配置效果] 作为前端请求后端 API 的 base URL；配置错误会导致所有接口请求失败。
# [格式/默认值] URL 字符串；默认示例：http://127.0.0.1:8080
# 注意：变量名必须以 VITE_ 前缀，Vite 才会注入到 import.meta.env
VITE_API_BASE_URL=http://127.0.0.1:8080
